<head>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Oxygen&display=swap" rel="stylesheet">
</head>

<p class="notice"><%= notice %></p>
<%= stylesheet_link_tag('application') %>
<%= stylesheet_link_tag('calendar') %>

<div class="const">
  <h1>Exams</h1>

</div>

<!--
<div id="exams">
  <table>
    <tr>
      <th><strong>Course</strong></th>
      <th><strong>Exam Date</strong></th>
      <th><strong>Exam Grade</strong></th>
      <th></th>
    </tr>
    <%# <% @exams.each do |exam| %> %>
      <tr>
        <%# <td><%= exam.course.course_name %></td> %>
        <%# <td><%= exam.exam_date.strftime("%m/%d/%y") %></td> %>
        <%# <td><%= exam.exam_grade %></td> %>
        <%# <td><%= button_to "SHOW", exam, method: :get, class: "show-btn" %></td> %>
        <%# <% end %>  %>
      </tr>
  </table>
</div>
 -->

<div id="calendar">
  <%# <%= if  %>
<%= month_calendar(exams: @exam_by_month) do |date, exams| %>
  <%= date.day %>
  <% @exam_by_month.each do |exam| %>
    <div>
      <% if date == exam.exam_date %>
        <%= link_to exam.course.course_name, exams_path %>
      <% end %>
    </div>
  <% end %>
<% end %>
</div>

<div class = "newRecord">
  <%= button_to "New Exam", new_exam_path, method: :get, class: "new-and-back-btn"%>
</div>
